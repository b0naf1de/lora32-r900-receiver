# Local dev credentials, if any (the firmware needs no secrets file — all config is runtime via the portal)
src/secrets.h

# PlatformIO build output + downloaded libraries
.pio/

# Python
**/__pycache__/
*.pyc

# Captured RF data / experiment runs (may contain neighbours' meter readings)
experiments/results/

# UI prototypes + captured portal screenshots (contain real SSIDs/creds/meter ids — keep local only)
experiments/ui/

# OS
.DS_Store
